'use strict';

export default {

	methods: {

		/**
		 * handles errors
		 *
		 * @param {Object|String} err
		 *
		 * @author shad
		 */
		handlingErrors( err ) {

			this.handlingErrors( err );

			// TODO handle specific errors
			// if ( err.code === 401 ) {
			// 	this.$router.go( { name: 'login' } );
			// }
			//
			// if ( err.code === 403 ) {
			// 	this.$router.go( { name: 'not_authorized' } );
			// }
			//
			// if ( err.code === 404 ) {
			// 	this.$router.go( { name: 'not_found' } );
			// }

		}

	}

};
